export function resolvePayJoyLocator(customer,sales){const sale=sales.find(item=>item.customer_id===customer.id&&item.platform==='PayJoy');if(!sale)return{sale:null,type:'NONE',value:null};for(const[type,key]of[['IMEI','imei_internal'],['PHONE','phone_internal'],['DEVICE_TAG','device_tag_internal'],['PAYJOY_CUSTOMER_ID','external_customer_id']])if(sale[key])return{sale,type,value:sale[key]};return{sale,type:'NONE',value:null};}
