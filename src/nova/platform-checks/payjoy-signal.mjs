export function createPayJoySignal(observed){if(!observed?.match)return{signal:'YELLOW',reason_code:'PAYJOY_NOT_FOUND'};if(observed.contract_present&&observed.remaining_balance>0&&observed.payment_history_available&&observed.payment_count===0)return{signal:'RED',reason_code:'ACTIVE_BALANCE_WITHOUT_OBSERVED_PAYMENTS'};if(observed.payment_history_available&&observed.payment_count>0&&observed.last_payment_at)return{signal:'GREEN',reason_code:'PAYMENT_HISTORY_OBSERVED'};return{signal:'YELLOW',reason_code:'INCOMPLETE_EVIDENCE'};}
